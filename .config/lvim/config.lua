-- basic settings
reload("user.builtins")
reload("user.extra")
reload("user.alpha")

-- settings for specific types of files
reload("user.other-setup")
reload("user.c-starter")
reload("user.py-starter")
reload("user.rust-starter")

-- plugins
reload("user.smoothie")
reload("user.todo-comments")
reload("user.colorizer")
reload("user.spectre")
reload("user.noice")
